{% extends 'back/base_back_etape.html.twig' %}

{% block title %}Etape{% endblock %}

{% block body %}

<style>

#etape {
	background-color: Snow;
	margin-left: 50px;
	width: 70%;
}

#etape th {
	background-color: #ff4d4d;
	padding: 20px;
	border: 2px solid black;
	width: 20%;
}

#etape td {
	border: 1px solid black;
	}

#etape tr:nth-child(even){background-color: #e6f7ff;}




</style>


    <h1>Etape</h1>

    <table class="table" id="etape">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etape.id }}</td>
            </tr>
            <tr>
                <th>NumeroEtape</th>
                <td>{{ etape.numeroEtape }}</td>
            </tr>
            <tr>
                <th>VilleEtape</th>
                <td>{{ etape.villeEtape }}</td>
            </tr>
            <tr>
                <th>NombreJours</th>
                <td>{{ etape.nombreJours }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_etape_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_etape_edit', {'id': etape.id}) }}">Modifier</a>
    
{# 	<a href="{{ path('admin_etape_delete', {'id': etape.id}) }}">Supprimer</a>#}
{{ include('back/etape/_delete_form.html.twig')}}

    
{% endblock %}
